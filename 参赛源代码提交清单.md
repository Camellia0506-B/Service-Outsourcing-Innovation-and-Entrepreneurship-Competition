# 参赛源代码提交清单

本清单仅保留“作品团队开发产生的源代码”，排除开发工具、测试产物、说明文档、第三方依赖与构建输出。

## 一、建议提交（源代码）

### 1) 后端 Java 源代码
- `backend/src/main/java/**`
- `backend/src/main/resources/application.yml`
- `backend/src/main/resources/application-local.yml.example`
- `backend/src/main/resources/schema-*.sql`
- `backend/pom.xml`

### 2) AI 服务 Python 源代码
- `ai/**/*.py`
- `ai/requirements.txt`

### 3) 前端源代码
- `frontend/**/*.html`
- `frontend/**/*.css`
- `frontend/**/*.js`

### 4) 启动脚本（如评审需要本地运行）
- `start.bat`

## 二、明确不提交（非参赛源代码）

### 1) 文档与说明材料
- `docs/**`（包含 API 文档、测试报告、验证说明、归档文件等）

### 2) 测试代码与测试请求文件
- `backend/src/test/**`
- `**/*test*.*`
- `docs/tools/test_*.py`
- `docs/tools/*.http`

### 3) 开发工具与第三方依赖/构建输出
- `.git/**`
- `.idea/**`
- `.vscode/**`
- `**/node_modules/**`
- `backend/target/**`
- `**/venv/**`
- `**/__pycache__/**`
- `*.pyc`

### 4) 日志与临时文件
- `**/logs/**`
- `*.log`

### 5) 数据文件（通常不是“源代码”）
- `ai/data/**`（如评审要求附样例数据，可单独提供最小必要样本）

## 三、当前仓库可直接用于参赛提交的最小集合

1. `backend/src/main/java/**`
2. `backend/src/main/resources/**`（仅配置与建表/迁移 SQL）
3. `backend/pom.xml`
4. `ai/**/*.py`
5. `ai/requirements.txt`
6. `frontend/**/*.html`
7. `frontend/**/*.css`
8. `frontend/**/*.js`
9. `start.bat`（可选）

## 四、Windows 快速打包参考（可选）

在仓库根目录执行（PowerShell）：

```powershell
$dest = "E:\gradquest_submission_source"
if (Test-Path $dest) { Remove-Item $dest -Recurse -Force }
New-Item -ItemType Directory -Path $dest | Out-Null

# backend
New-Item -ItemType Directory -Path "$dest\backend\src\main" -Force | Out-Null
Copy-Item "backend\src\main\java" "$dest\backend\src\main\" -Recurse -Force
Copy-Item "backend\src\main\resources" "$dest\backend\src\main\" -Recurse -Force
Copy-Item "backend\pom.xml" "$dest\backend\" -Force

# ai
Copy-Item "ai" "$dest\" -Recurse -Force
Remove-Item "$dest\ai\venv" -Recurse -Force -ErrorAction SilentlyContinue
Remove-Item "$dest\ai\data" -Recurse -Force -ErrorAction SilentlyContinue
Get-ChildItem "$dest\ai" -Recurse -Include "__pycache__" | Remove-Item -Recurse -Force -ErrorAction SilentlyContinue
Get-ChildItem "$dest\ai" -Recurse -Include "*.pyc" | Remove-Item -Force -ErrorAction SilentlyContinue

# frontend
Copy-Item "frontend" "$dest\" -Recurse -Force
Remove-Item "$dest\frontend\node_modules" -Recurse -Force -ErrorAction SilentlyContinue

# optional startup script
Copy-Item "start.bat" "$dest\" -Force -ErrorAction SilentlyContinue
```

> 说明：若比赛要求“仅源代码，不含数据与脚本”，可再去掉 `start.bat` 与 `backend/src/main/resources` 中非源码配置文件。
