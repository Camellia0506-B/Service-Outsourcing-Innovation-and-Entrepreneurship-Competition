# 本机 MySQL 配置（覆盖默认）
# 使用步骤：
# 1. 复制此文件为 application-local.yml（同目录下）
# 2. 把下面的 username、password 改成你本机 MySQL 的账号密码
# 3. 启动后端时加参数：mvn spring-boot:run -Dspring.profiles.active=local

spring:
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/gradquest?useSSL=false&serverTimezone=Asia/Shanghai&useUnicode=true&characterEncoding=utf8
    username: root
    password: 这里填你的MySQL密码
